import * as React from "react";

const Feed: React.FC = () => {
  // Feed gets updates within the last day from your followed people and lists them here.
  // If nothing is found, it looks to the previous day.

  return (
    <div>
      <h2>Your Feed</h2>
    </div>
  );
};
